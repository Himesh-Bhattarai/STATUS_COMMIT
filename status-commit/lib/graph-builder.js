// Placeholder for optional dependency graph and drift detection.
// Intentionally minimal for npm packaging; can be extended with madge/tree-sitter.

export async function buildGraph() {
  return { edges: [], nodes: [], cycles: 0 };
}
